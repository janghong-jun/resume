import { defineConfig } from "vitepress";

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "Jang Hong Jun Resume",
  description: "MD Resume",
  base: "/resume/", // ← 저장소명
});
